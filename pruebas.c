#include <stdio.h>
#include <string.h>
#include <stdlib.h>


//#include "estructuras.h"

/*
int main (){
    FILE *arch;
    Pagos pag; 
    



///* 

        if((arch=fopen("pagos.bin","r+b"))!=NULL){
            fread(&pag,sizeof(pag),1,arch);
            while (!feof(arch)){
                printf("\n");
                printf("%d",pag.dni);
                printf("\n");
                printf("%d",pag.estado);
                printf("\n");
                pag.estado = 0;
                printf("%d",pag.estado);

            
            //fseek(arch,sizeof(pag)*(-1),SEEK_CUR);
            //fwrite(&pag,sizeof(pag),1,arch);
            //fseek(arch,sizeof(pag),SEEK_END);
            fread(&pag,sizeof(pag),1,arch);
		    
            }
        fclose(arch);
        }
}

*/